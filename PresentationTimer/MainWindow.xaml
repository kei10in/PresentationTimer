<Window x:Class="PresentationTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pt="clr-namespace:PresentationTimer"
        xmlns:vm="clr-namespace:PresentationTimer.ViewModels"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Window.Resources>
        <pt:SimpleTimeSpanConverter x:Key="TimeSpanConverter" />
    </Window.Resources>
    <Grid Background="Black">
        <Viewbox Margin="12" Name="viewbox1" Stretch="Uniform">
            <TextBlock Text="{Binding Path=TimeRemaining, Converter={StaticResource TimeSpanConverter}}" Name="curerntTime" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" />
        </Viewbox>
        <Grid VerticalAlignment="Bottom">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Content="{Binding Path=StartStop}" Height="23" Width="75" Command="{Binding Path=StartStopCommand}" Margin="6,12,0,12" />
                <Button Content="Reset" Height="23" Width="75" Command="{Binding Path=ResetCommand}" Margin="6,12,12,12" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
